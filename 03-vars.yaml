- name: play level variables
  hosts: local
  connection : local
  vars :
    course : "Devops with Aws"
    Duration : "140 hrs"
  tasks:
  - name: print variables
    ansible.builtin.debug :
     msg : "Course is : {{course}} with {{Duration}}" 
    